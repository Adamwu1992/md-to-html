
/**
 * 'a|bcde' => 'a|b.c.d.e*'
 * @param input 
 */
function preParse(input: String): String {
    let result = '';
    for(let i = 0; i < input.length - 1; i++) {
        const next = input[i + 1];
    }
    return result;
}